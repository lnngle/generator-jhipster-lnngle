import { HttpClientTestingModule } from '@angular/common/http/testing';
import { waitForAsync, ComponentFixture, TestBed } from '@angular/core/testing';
import { RouterTestingModule } from '@angular/router/testing';
import { DelonFormModule } from '@delon/form';
import { SharedModule } from '@shared';
import { NzMessageService } from 'ng-zorro-antd/message';

import { Entities<%= entityAngularName %>Component } from './<%= entityFileName %>.component';

describe('Entities<%= entityAngularName %>Component', () => {
  let component: Entities<%= entityAngularName %>Component;
  let fixture: ComponentFixture<Entities<%= entityAngularName %>Component>;

  beforeEach(
    waitForAsync(() => {
      TestBed.configureTestingModule({
        imports: [HttpClientTestingModule, SharedModule, RouterTestingModule, DelonFormModule.forRoot()],
        declarations: [Entities<%= entityAngularName %>Component],
        providers: [NzMessageService]
      }).compileComponents();
    })
  );

  beforeEach(() => {
    fixture = TestBed.createComponent(Entities<%= entityAngularName %>Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
